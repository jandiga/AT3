BEGIN Login_SignUp_Process

FUNCTION SignUp()
    # Display form and collect user details (name, email, password, role, classCode).
    # Validate email format and password strength.
    # Assign default permissions based on role.
    # Store user details securely and send an email confirmation.
    # If confirmation is successful and role is "Student," initiate user profile creation.
END FUNCTION

FUNCTION hash(password)
    # Use bcrypt with salt=12 for password hashing.
END FUNCTION

CLASS Session
    ATTRIBUTES:
        sessionToken, expiry, IPValidation
END CLASS

FUNCTION ValidateEmail(email)
    # Check if the email is in a valid format; return TRUE or FALSE.
END FUNCTION

FUNCTION CheckPasswordStrength(password)
    # Ensure password meets length and complexity requirements; return TRUE or FALSE.
END FUNCTION

FUNCTION SendEmailConfirmation(email)
    # Generate a unique token, store it, and send a confirmation email.
END FUNCTION

FUNCTION Login()
    # Display login form and collect user credentials.
    # Validate credentials and check CAPTCHA before granting access.
END FUNCTION

FUNCTION ValidateCredentials(email, password)
    # Fetch stored hashed password from the database.
    # Compare input password (hashed) with stored password.
    # Return TRUE if valid, otherwise FALSE.
END FUNCTION

FUNCTION ForgotPassword()
    # Display password reset form and collect email.
    # If email exists, generate a reset token, store it, and send a reset email.
END FUNCTION

FUNCTION ResetPassword(token, newPassword)
    # Verify token validity, update password in the database, and confirm success.
END FUNCTION

END Login_SignUp_Process
