BEGIN Permissions
  FUNCTION CheckPermissions(user, action)
    SWITCH action:
      CASE 'create_league':
        RETURN user.role == "Teacher"
      CASE 'draft_player':
        RETURN "team_management" IN user.permissions
      CASE 'update_grades':
        RETURN user.role == "Teacher"
END Permissions